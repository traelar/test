{
  "name": "billnest-cloudflare-backend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute billnest-db --remote --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.68.0"
  }
}
